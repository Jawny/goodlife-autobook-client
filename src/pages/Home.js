import React, { Fragment } from "react";
import { LoginButton, SignUpButton } from "../components/index";

const Home = () => {
  return (
    <Fragment>
      <LoginButton />
      <SignUpButton />
    </Fragment>
  );
};

export default Home;
